class Solution {
public:
    bool isPalindrome(int x) {
        string s = to_string(x), str = to_string(x);
        reverse(s.begin(), s.end());

        if (s == str)
            return true;
        else
            return false;
    }
};
